# Django Settings
DJANGO_SECRET_KEY=django-insecure-dev-key-CHANGE-ME-IN-PRODUCTION-$(openssl rand -hex 32)
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# Database
DATABASE_URL=postgres://dignifi:dignifi_dev_password@db:5432/dignifi_dev
POSTGRES_DB=dignifi_dev
POSTGRES_USER=dignifi
POSTGRES_PASSWORD=dignifi_dev_password

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# Field Encryption (for PII: SSN, income data)
# IMPORTANT: Generate a secure key with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=e1JTx_q3coXgha6_6bbk7NPHQPsCFmWzc-YkZzghvxU=

# District Configuration
DEFAULT_DISTRICT=ilnd

# LLM (local llama.cpp — auto-started via docker compose up)
LLM_BASE_URL=http://llm:8080/v1

# Django Settings Module
DJANGO_SETTINGS_MODULE=config.settings.development
